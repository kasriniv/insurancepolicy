package org.acme.insurance;

rule "count Wheels"
//ruleflow-group "car"
dialect "mvel"
    when
        Car( $numWheels : getWheels())
    then
        System.out.println("Num Wheels: "+ $numWheels);
end